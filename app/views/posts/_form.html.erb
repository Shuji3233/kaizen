<%= simple_form_for @post do |f| %>
	<%= f.input :title, label: "題名" %>
	<%= f.input :body, label: "改善内容" %>
	<%= f.input :cost, label: "必要経費" %>
	<%= f.input :benefits, label: "効果" %>
    <%= f.button :submit %>
<% end %>